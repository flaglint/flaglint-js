# Issue Tracker

FlagLint uses GitHub Issues at github.com/flaglint/flaglint/issues.

When creating issues:
- Use `gh issue create` 
- Apply labels: bug, enhancement, docs, adr, chore
- One vertical slice per issue — no mega-issues
